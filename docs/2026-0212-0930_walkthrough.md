# ウォークスルー: ホワイトリスト/ブラックリストモード切替機能

Issue #1 + #3 対応完了。

## 変更内容

### 型定義 ([index.ts](file:///home/kimura512/dev/ctrlEnterSenderA/src/types/index.ts))

- `ActivationMode` 型（`'blacklist' | 'whitelist'`）追加
- `StorageSchema.activationMode` フィールド追加

### ストレージ ([storage.ts](file:///home/kimura512/dev/ctrlEnterSenderA/src/background/storage.ts))

- `getActivationMode()` / `setActivationMode()` 追加
- `getDomainConfig()`: ホワイトリスト時はデフォルト `enabled: false`
- `getAllConfigs()`: ホワイトリスト時はデフォルト無効ドメインを挿入しない

### ポップアップ ([App.tsx](file:///home/kimura512/dev/ctrlEnterSenderA/src/popup/App.tsx))

- モードインジケーター（Blacklist / Whitelist）表示追加

### オプションページ ([App.tsx](file:///home/kimura512/dev/ctrlEnterSenderA/src/options/App.tsx))

- モード切替セクション（ラジオボタン＋確認ダイアログ）追加
- ホワイトリスト時は「初期設定済みドメイン」セクション非表示

### i18n

- 全34言語に9つの新メッセージキーを追加

## 検証結果

- ✅ `pnpm build` 成功（TypeScript + Vite）
- ✅ 47モジュール正常変換
- ⚠️ Chrome拡張としての動作テストはユーザーによる手動確認が必要

## 後方互換性

- `activationMode` 未設定 = `'blacklist'`（既存ユーザーの動作は変わらない）
- マイグレーション不要
